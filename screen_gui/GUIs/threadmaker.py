from threading import RLock
import sys
sys.path.append("..")
from py2uart import UART
import csv

class Threader():

    """Threadr class will have a lock creater, thread creater, 
       a lock acquirer and operation implementer, a lock releaser"""

    def __init__():
    	self.v = []
        

    def serial_start():
    
        try:
            lock.acquire()
            self.v = self.v.append(randgen())

            if self.v[len(self.v)] == 'x'


        finally:
            lock.release()

    def acquire():
